= link_to 'Ask question', new_question_path

h1 List of Questions

- if not @questions.blank?
  ul
    - @questions.each do |question|
      li = link_to question.title, question
- else
  p There is no questions