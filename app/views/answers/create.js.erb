<% if @answer.errors.present? %>
  $('.answer_errors').html('<%= j render "application/error_messages", target: @answer %>');
<% else %>
  // New answer is appended via ActionCable now
  // $('.answers').append('<%= j render @answer %>');

  $('.answer_errors').html('');
  document.getElementById("new_answer").reset();
<% end %>